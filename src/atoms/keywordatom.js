import { atom } from "recoil";

export const keywordatom = atom({
    key: "keywordatom",
    default: "",
});
export const ChatBotState = atom({
    key: "chatstate",
    default: false, // Set default to false (boolean)
});
